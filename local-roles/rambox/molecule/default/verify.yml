---
- name: Verify
  hosts: all
  become: true

  tasks:
    - name: Gather package facts
      ansible.builtin.package_facts:
        manager: auto

    - name: Assert rambox is installed
      ansible.builtin.assert:
        that:
          - "'rambox' in ansible_facts.packages"

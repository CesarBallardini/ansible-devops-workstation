---
# See https://github.com/ramboxapp/community-edition/releases for versions

- name: paquetes generales
  ansible.builtin.package:
    name:
      - jq
      - git
      - wget
      - curl
      - unzip
    state: present
  become: true
  tags:
    - rambox

- name: rambox | version actual instalada
  ansible.builtin.shell: |
    LANG=C dpkg -l rambox | awk ' /^ii/ {print gensub("([0-9]*\\.[0-9]*\\.[0-9]*)-(.*)", "\\1","1", $3) }'
  register: reg_rambox_version
  failed_when: false
  changed_when: false
  tags:
    - rambox

- name: rambox | busca el nombre de la version actual
  ansible.builtin.shell: |
    curl --silent --fail \
      ${GITHUB_TOKEN:+-H "Authorization: token ${GITHUB_TOKEN}"} \
      https://api.github.com/repos/{{ rambox_github_repo }}/releases/latest \
    | jq -r ".tag_name // empty"
  register: reg_rambox_url_version
  changed_when: false
  failed_when: false
  tags:
    - rambox

- ansible.builtin.set_fact:
    rambox_version_instalada: "{{ reg_rambox_version.rc == 0 and reg_rambox_version.stdout or false }}"
    rambox_url_version: "{{ reg_rambox_url_version.stdout if reg_rambox_url_version.stdout | length > 0 else '' }}"
  tags:
    - rambox

- name: rambox | warn if version detection failed
  ansible.builtin.debug:
    msg: "WARNING: could not detect latest rambox version from GitHub API (rate limit?). Skipping rambox install/upgrade."
  when: rambox_url_version | length == 0
  tags:
    - rambox

- ansible.builtin.set_fact:
    rambox_url_actual: "https://github.com/{{ rambox_github_repo }}/releases/download/{{ rambox_url_version }}/Rambox-{{ rambox_url_version }}-linux-amd64.deb"
  when: rambox_url_version | length > 0
  tags:
    - rambox

- ansible.builtin.debug: msg="ramboxapp, version instalada - {{ rambox_version_instalada }}"
  tags:
    - rambox
- ansible.builtin.debug: msg="ramboxapp, url actual        - {{ rambox_url_actual }}"
  tags:
    - rambox
- ansible.builtin.debug: msg="ramboxapp, url version       - {{ rambox_url_version }}"
  tags:
    - rambox
# "

- name: rambox | descarga paquete
  ansible.builtin.get_url:
    url: "{{ rambox_url_actual }}"
    dest: "/tmp/{{ rambox_url_actual | basename }}"
    mode: "0755"
    force: true
  become: true
  when: rambox_url_version | length > 0 and rambox_version_instalada|default("0.0.0", true) is version(rambox_url_version, operator='lt', strict=True)
  tags:
    - rambox

- name: rambox | instala binario
  ansible.builtin.apt:
    deb: /tmp/{{ rambox_url_actual | basename }}
  become: true
  when: rambox_url_version | length > 0 and rambox_version_instalada|default("0.0.0", true) is version(rambox_url_version, operator='lt', strict=True)
  tags:
    - rambox

- name: rambox | Install missing packages
  ansible.builtin.shell: |
    apt install -f -y
  become: true
  when: rambox_url_version | length > 0 and rambox_version_instalada|default("0.0.0", true) is version(rambox_url_version, operator='lt', strict=True)
  tags:
    - rambox

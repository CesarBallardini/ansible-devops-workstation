---
- name: Verify
  hosts: all
  become: true

  tasks:
    - name: Gather package facts
      ansible.builtin.package_facts:
        manager: auto

    - name: Assert apt-fast is installed
      ansible.builtin.assert:
        that:
          - "'apt-fast' in ansible_facts.packages"

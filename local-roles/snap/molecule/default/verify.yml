---
- name: Verify
  hosts: all
  become: true

  tasks:
    - name: Gather package facts
      ansible.builtin.package_facts:
        manager: auto

    - name: Assert snapd is installed
      ansible.builtin.assert:
        that:
          - "'snapd' in ansible_facts.packages"

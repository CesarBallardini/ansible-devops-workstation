---
- name: Verify
  hosts: all
  become: true

  tasks:
    - name: Gather package facts
      ansible.builtin.package_facts:
        manager: auto

    - name: Assert gcm is installed
      ansible.builtin.assert:
        that:
          - "'gcm' in ansible_facts.packages"
